// Enum
import { PlaceName } from 'interfaces/enum/map';
// Interface
import { IMarkerProps } from 'interfaces/interface/map';

export type TNamePlace = PlaceName.Mikhailovka;

export type TAddMarkerWithPopupToMap = (props: IMarkerProps) => any;
